# Прогресс проекта Carpool Queue

Здесь фиксируем, что уже сделано, чтобы не терять контекст между сессиями.

## ✅ Шаг 1: Каркас проекта
- Создана структура папок (`app/`, `deploy/`)
- Минимальное FastAPI-приложение (`app/main.py`), отвечает `{"status": "ok", ...}` на `/`
- `requirements.txt` с базовыми зависимостями (fastapi, uvicorn, sqlmodel, alembic, python-dotenv, passlib)
- `.gitignore`, `.env.example`, `README.md`
- Проверено локально: `uvicorn app.main:app --reload` → страница отвечает корректно
- Python 3.13.3 в venv (совпадает с версией на сервере)

## ✅ Шаг 2: Приватный репозиторий на GitHub
- Репозиторий создан: https://github.com/glorden/carpool-queue
- Локальный git инициализирован, сделан первый коммит
- Настроена авторизация через браузер (git credential manager)
- Код запушен в ветку `main`

## ✅ Шаг 3: Модели БД + первая Alembic-миграция
- Созданы модели: `User`, `QueuePosition`, `Order`, `OrderOffer` (`app/models/`)
- Подключение SQLModel + SQLite настроено (`app/database.py`, `app/config.py`)
- Alembic инициализирован, подключён к моделям через `env.py` (target_metadata = SQLModel.metadata)
- `alembic.ini` настроен на `sqlite:///./carpool.db`
- Сгенерирована и применена первая миграция (`create initial tables`)
- Проверено: все таблицы созданы в `carpool.db` — `user`, `order`, `queueposition`, `orderoffer`, `alembic_version`

## ✅ Шаг 4: Очередь (чтение) — GET /queue
- Добавлен эндпоинт `GET /queue` в `app/main.py`
- Делает JOIN `QueuePosition` + `User`, сортирует по `position`
- Возвращает список вида: `{"position": ..., "user_id": ..., "name": ...}`
- Создан скрипт `scripts/seed.py` для наполнения БД тестовыми пользователями
  (запуск: `python -m scripts.seed`)
- Проверено локально через браузер и `/docs` — эндпоинт возвращает корректный
  список пользователей в порядке очереди

## ✅ Шаг 5: Создание заказа — POST /orders
- Добавлен эндпоинт `POST /orders` в `app/main.py`
- Принимает `route` и `comment` (оба поля опциональны)
- Создаёт `Order` со статусом `pending`, `assigned_to = None`
- НЕ создаёт `OrderOffer` — предложение первому в очереди будет
  отдельным шагом (Шаг 6, respond)
- Проверено локально через `/docs` — заказ создаётся корректно,
  возвращается с присвоенным `id`
  
## Уточнение к Шагу 5 / подготовка к Шагу 6
- POST /orders теперь при создании заказа сразу создаёт OrderOffer
  для первого пользователя в очереди (раньше договаривались, что это
  будет отдельным шагом — по факту оказалось логичнее делать сразу,
  т.к. заказ без предложения первому не имеет смысла как отдельное
  состояние)
- Проверено локально: после создания заказа в таблице order_offer
  появляется запись с response=pending для того, кто сейчас первый
  по очереди

## ✅ Шаг 6: Принятие/отказ — POST /orders/{id}/respond
- Добавлен эндпоинт POST /orders/{order_id}/respond в app/main.py
- Принимает user_id и response ("accepted" или "declined")
- Находит актуальный pending-OrderOffer для заказа
- При accepted: OrderOffer -> accepted, Order.status -> assigned,
  Order.assigned_to = user_id, пользователь уходит в конец очереди
  (максимальная позиция + 1)
- При declined: OrderOffer -> declined, Order остаётся pending,
  автоматически создаётся новый OrderOffer для следующего по очереди
  (по кругу, если отказавшийся был последним)
- Проверено локально через /docs: и accepted, и declined отработали
  корректно, очередь и статусы заказа меняются как задумано
  
✅ Принятие/отказ — POST /orders/{id}/respond
🔄 Завершение заказа — POST /orders/{id}/complete ← следующий шаг

## Ещё впереди (по плану)
- Шаг 7: Завершение заказа — `/orders/{id}/complete`
- Шаг 8: История заказов — `/orders GET`
- Шаг 9: Простой интерфейс (HTML-страницы)
- Шаг 10: Деплой (Caddyfile, systemd unit, сервер)

## Логика очереди (напоминание, чтобы не забыть при разработке)
- Заказ предлагается первому в очереди
- Принял → выполнил → ушёл в конец очереди
- Отказался → остаётся на своей позиции, заказ уходит следующему
- Таймаутов на ответ нет — ждём сколько угодно, коммуникация идёт в личном чате участников

## Архитектурные решения и упрощения
- **POST /orders/{id}/respond (Шаг 6):** эндпоинт НЕ проверяет, что отвечает
  именно тот пользователь, которому реально было отправлено предложение
  (нет проверки "это твоя очередь"). Любой user_id может принять/отклонить
  любой pending-заказ. Осознанное упрощение: группа доверенных людей (10 чел),
  договорённости идут в личном чате, риск злоупотребления минимален.
  Если понадобится ужесточить — добавить проверку активного OrderOffer
  с response=pending именно для этого user_id.