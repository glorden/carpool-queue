# Прогресс проекта Carpool Queue

Здесь фиксируем, что уже сделано, чтобы не терять контекст между сессиями.
Архитектурные решения, технический долг и логика очереди — в [ARCHITECTURE.md](./ARCHITECTURE.md).

## ✅ Шаг 1: Каркас проекта
- Структура папок (`app/`, `deploy/`), минимальное FastAPI-приложение
- `requirements.txt`, `.gitignore`, `.env.example`, `README.md`
- Проверено локально: `uvicorn app.main:app --reload` → отвечает корректно
- Python 3.13.3 в venv (совпадает с версией на сервере)

## ✅ Шаг 2: Приватный репозиторий на GitHub
- Репозиторий: https://github.com/glorden/carpool-queue
- Git инициализирован, авторизация через браузер, код запушен в `main`

## ✅ Шаг 3: Модели БД + первая Alembic-миграция
- Модели: `User`, `QueuePosition`, `Order`, `OrderOffer` (`app/models/`)
- SQLModel + SQLite (`app/database.py`, `app/config.py`)
- Alembic подключён к моделям, первая миграция применена
- Проверено: все таблицы созданы в `carpool.db`

## ✅ Шаг 4: Очередь (чтение) — GET /queue
- JOIN `QueuePosition` + `User`, сортировка по `position`
- Скрипт `scripts/seed.py` для тестовых пользователей
- Проверено локально через браузер и `/docs`

## ✅ Шаг 5: Создание заказа — POST /orders
- Принимает `route` и `comment` (опциональны)
- Создаёт `Order` со статусом `pending`
- Уточнение: сразу создаёт `OrderOffer` для первого в очереди
  (см. ARCHITECTURE.md)
- Проверено локально через `/docs`

## ✅ Шаг 6: Принятие/отказ — POST /orders/{id}/respond
- Принимает `user_id` и `response` (`accepted`/`declined`)
- При `accepted`: заказ назначается, пользователь сразу уходит в конец очереди
- При `declined`: оффер уходит следующему по очереди (по кругу)
- Проверено локально через `/docs`, оба сценария корректны

## ✅ Шаг 7: Завершение заказа — POST /orders/{id}/complete
- Проверяет `Order.status == assigned`, иначе `400`
- При успехе: `status -> completed`, `completed_at = now()`
- Рефакторинг ошибок: переход на `HTTPException` вместо `{"error": ...}`
  (см. ARCHITECTURE.md)
- Проверено локально через `/docs` и `curl`, все edge-кейсы корректны

## ✅ Шаг 8: История заказов — GET /orders
- Query-параметры: `status`, `user_id`, `limit` (default=100, max=500)
- Сортировка `created_at DESC`, без полноценной пагинации
- Проверено локально через `curl`: без фильтров и с разными комбинациями

## ✅ Шаг 9.1: GET /orders/pending (бэкенд)
- Read-only эндпоинт: `pending`-заказы + данные активного `OrderOffer`
- Аддитивное изменение, ничего не сломало
- Проверено вручную через `curl` полным сценарием (создание → отклонение →
  переход оффера → принятие → уход из очереди)

## ✅ Шаг 9.2: подключение статики в FastAPI
- `app.mount("/static", StaticFiles(...), name="static")`
- Структура папок `static/`, `static/css/`, `static/js/`
- Проверено: `GET /static/` отдаёт HTML, `GET /` — health-check JSON

## ✅ Шаг 9.3: дэшборд

Реализован по мини-шагам:

- ✅ **9.3.1** — HTML-каркас, селектор пользователя (`localStorage`),
  вывод очереди из `GET /queue`, подсветка текущего пользователя
- ✅ **9.3.2** — форма создания заказа (`POST /orders`), список активных
  предложений (`GET /orders/pending`), обновление без перезагрузки страницы
- ✅ **9.3.3** — кнопки принять/отклонить (показываются только если оффер
  адресован выбранному пользователю) и завершить (список "мои заказы
  в пути" через `GET /orders?status=assigned&user_id=X`)

## ✅ Шаг 9.4: страница истории заказов

- Создана `static/history.html` — отдельная страница с таблицей всех заказов
- Фильтры: `status` (select с вариантами pending/assigned/completed)
  и `user_id` (select с именами из `GET /queue`), применяются кнопкой
- Таблица использует уже готовый `GET /orders` с query-параметрами,
  бэкенд не менялся
- Взаимные ссылки между дэшбордом и историей в шапке ("История заказов →" / "← Дэшборд")
- Проверено: фильтры по отдельности и вместе работают корректно


## ✅ Шаг 9: Простой интерфейс (HTML) — завершён

### Разбивка на подшаги
- ✅ **9.1** — `GET /orders/pending` (бэкенд)
- ✅ **9.2** — подключение статики
- ✅ **9.3** — дэшборд (очередь, создание заказа, принять/отклонить/завершить)
- ✅ **9.4** — страница истории (таблица + фильтры)

## ✅ Шаг 10: Подготовка проекта к первому релизу — завершён

### Разбивка на подшаги
- ✅ **10.1** — русификация пользовательского интерфейса
- ✅ **10.2** — обновление README.md
- ✅ **10.3** — обновление PROGRESS.md
- ✅ **10.4** — создание CHANGELOG.md
- ✅ **10.5** — финальная проверка проекта перед деплоем

### Выполнено

- ✅ Полностью русифицирован пользовательский интерфейс
  (интерфейс, сообщения, статусы — единая терминология "водитель").
- ✅ Обновлён `README.md` перед первым релизом (описание, бизнес-логика,
  структура проекта, инструкция запуска, таблица API, ссылки на
  PROGRESS.md/ARCHITECTURE.md).
- ✅ Создан `CHANGELOG.md` (формат Keep a Changelog), первая версия
  зафиксирована как `0.1.0`.
- ✅ Финальная проверка по чеклисту (Шаг 10.5):
  - код и структура — чисто, без debug-кода и мёртвых веток;
    `.gitignore` и `.env.example` в порядке
  - БД/миграции — `alembic upgrade head` и `scripts/seed.py`
    проверены на чистой базе с нуля
  - полный сквозной прогон API-сценария (создание заказа → отказ →
    переход оффера по кругу → принятие → уход в конец очереди →
    завершение → фильтрация в истории) — все шаги корректны
  - веб-интерфейс (дэшборд + история) проверен вручную в браузере
  - документация сверена с кодом; в `ARCHITECTURE.md` найдена и
    исправлена порча кодировки (несколько символов)
  - `git status` чистый, всё запушено
  - создан и запушен тег релиза **`v0.1.0`**

### Проект зафиксирован как релиз v0.1.0 (2026-07-12)

## ✅ Шаг 11: деплой на VPS — завершён

- Сервер: Ubuntu 24.04 LTS, `109.248.160.140`, домен `zakaz.glorden.ru`
- Решение по техническому долгу (identity-проверки в `/respond` и
  `/complete`): оставлен как есть — осознанное упрощение для доверенной
  группы из 10 человек (см. ARCHITECTURE.md), деплой не блокирует
- Система обновлена (`apt update && upgrade`), перезагрузка применена
- Приложение работает под отдельным непривилегированным пользователем
  `deploy`, не под root
- Стек: systemd (автозапуск + restart on failure) + Caddy (reverse proxy,
  автоматический HTTPS через Let's Encrypt)
- Firewall (`ufw`): открыты только 22/80/443, порт приложения (8000)
  наружу не смотрит
- Доступ к приватному репозиторию — через read-only GitHub deploy key,
  сгенерированный на сервере
- На сервере — чистая БД (миграции `alembic upgrade head` с нуля,
  локальные тестовые данные не переносились)
- Проверено: `https://zakaz.glorden.ru/` (health-check), дэшборд и
  история открываются в браузере, `GET /queue` отвечает
- Полная инструкция по установке и обновлению — [DEPLOY.md](./DEPLOY.md)

### Проект зафиксирован как задеплоенный на прод (2026-07-22)

## ✅ Шаг 12: страница «Прайс» и редактируемый прайс-лист — завершён

- Добавлена страница `static/price.html`: туры, направления (102 шт.),
  бизнес-класс, минивэны — изначально статика, затем перенесено в БД
- Единая навигация между страницами вынесена в `static/js/nav.js`
  (одна строка под заголовком на каждой странице), добавлен
  `STYLEGUIDE.md` с базовыми соглашениями
- Живой поиск по названию направления на клиенте, без бэкенда
- HTTP Basic Auth в Caddy закрывает весь сайт целиком, включая все
  API-эндпоинты (проверено на `/queue`, `/orders`, `/users`, `/health`)
- Прайс переведён из статического HTML в БД: модели `PriceItem` и
  `PriceLogEntry`, CRUD-эндпоинты `GET/POST/PUT/DELETE /price` —
  добавлять/менять/удалять позиции может любой пользователь (та же
  модель доступа, что у `/respond`/`/complete`, см. ARCHITECTURE.md)
- Каждое изменение логируется (`GET /price/log`) и видно прямо на
  странице «Прайс» в свёрнутом блоке «История изменений»
- Data-миграция перенесла в БД текущие 112 позиций без потерь

## ✅ Шаг 13: отмена заказа — завершён
- `POST /orders/{id}/cancel`: работает для `pending`/`assigned`,
  из `completed`/`cancelled` — `400`
- Если заказ был `assigned` — водитель возвращается в **начало** очереди
  (не туда, откуда ушёл), см. ARCHITECTURE.md
- Кнопка «Отмена заказа» — и в блоке «Заказы, ожидающие ответа», и в
  «Мои заказы в пути» (одна и та же модель доступа, что у `/respond`/`/complete`)
- Добавлен статус `cancelled` в фильтр и таблицу страницы истории

## ✅ Шаг 14: уведомления водителям через VK — завершён
- Бэкенд: `app/notifications.py`, вызывается при создании заказа
  и при переходе оффера следующему по очереди (см. ARCHITECTURE.md)
- Сообщество «Горизонт. Едем далеко» добавлено участником в чат
  «Заказ» (там же постепенно собираются все водители)
- Особенность VK, с которой столкнулись: беседа не видна боту через
  API, пока в ней не отправлено хотя бы одно сообщение после
  добавления бота — учитывать при подключении новых чатов в будущем
- `VK_GROUP_TOKEN` и `VK_PEER_ID` настроены в `.env` на проде
- Живой тест на проде пройден: заказ создан → сообщение "Заказ #3
  (...) предложен: Антон Саныч" пришло в чат «Заказ» → тестовый
  заказ отменён, очередь не затронута
- Доработка: в сообщение добавлена ссылка на сайт (`SITE_URL`),
  водитель тегается по `User.vk_id`, если он известен (миграция
  `296fad1e2c65`); `vk_id` для всех 9 пользователей на проде
  сопоставлен по имени с участниками чата «Заказ»

## ✅ Шаг 15: ревью архитектуры + точечные правки — завершён
- Проведён полный обзор кода и документации (см. ARCHITECTURE.md,
  README.md, PROGRESS.md, CHANGELOG.md, DEPLOY.md на соответствие коду)
- Исправлено: `static/js/history.js` строил строки таблицы через
  `innerHTML` с прямой подстановкой `route`/`comment` — потенциальный
  stored XSS через свободный текст заказа. Переписано на
  `createElement`/`textContent`, как уже было в `dashboard.js`
- Исправлено: `OrderStatus.unassigned` был мёртвым кодом — ни один
  сценарий его не выставлял (отказ всегда уходит по кругу следующему).
  Убран из `app/models/order.py` и всех упоминаний (README/ARCHITECTURE/
  фильтр истории `history.html`/`history.js`). БД не требует миграции —
  колонка `status` в SQLite хранится как `VARCHAR`, без `CHECK`-ограничения
- Убрано отображение `(позиция N)` рядом с именем в очереди на дэшборде
  (`static/js/dashboard.js`) — номер и так виден из нумерации `<ol>`,
  а сырое значение `position` не идёт подряд без пропусков и путало
- Почищен `CHANGELOG.md`: убраны буквальные экранирующие слэши
  (`\#`, `\-`, `\[`), формулировки сокращены, детали — в PROGRESS.md

## ✅ Шаг 16: план identity-долга, бэкап БД, тесты на очередь — завершён
- ARCHITECTURE.md: явно сужен охват плана устранения identity-долга —
  только `/respond`/`/complete`; `/cancel`/`/price` вне охвата
  (обоснование см. в ARCHITECTURE.md). Добавлен раздел «на бумаге» —
  что понадобится проверить в 5 местах и что почти наверняка потребуется
  поле `Order.created_by`, если/когда появится полноценная авторизация
- Бэкап БД: `scripts/backup_db.sh`, ежедневный `sqlite3 .backup` через
  cron на проде. Хранение — гибрид: свежая копия всегда, более старые —
  по сетке «раз в 10 дней» от фиксированной даты (не растёт бесконечно,
  но остаются срезы за все периоды). Настройка cron — в DEPLOY.md
- Добавлены первые автотесты: `tests/test_queue_logic.py` (+
  `tests/conftest.py` с изолированной SQLite-БД на тест через
  `tmp_path` и подменой `get_session`). Покрыта самая хрупкая логика:
  принятие → в конец очереди, отказ → по кругу следующему (включая
  разворот с последнего на первого), отмена принятого → возврат вторым,
  не обгоняя текущего первого. `pytest`/`httpx` добавлены в
  `requirements.txt`, все 3 теста проходят локально