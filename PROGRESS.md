# Прогресс проекта Carpool Queue

Здесь фиксируем, что уже сделано, чтобы не терять контекст между сессиями.
Архитектурные решения, технический долг и логика очереди — в [ARCHITECTURE.md](./ARCHITECTURE.md).

## ✅ Шаг 1: Каркас проекта
- Структура папок (`app/`, `deploy/`), минимальное FastAPI-приложение
- `requirements.txt`, `.gitignore`, `.env.example`, `README.md`
- Проверено локально: `uvicorn app.main:app --reload` → отвечает корректно
- Python 3.13.3 в venv (совпадает с версией на сервере)

## ✅ Шаг 2: Приватный репозиторий на GitHub
- Репозиторий: https://github.com/glorden/carpool-queue
- Git инициализирован, авторизация через браузер, код запушен в `main`

## ✅ Шаг 3: Модели БД + первая Alembic-миграция
- Модели: `User`, `QueuePosition`, `Order`, `OrderOffer` (`app/models/`)
- SQLModel + SQLite (`app/database.py`, `app/config.py`)
- Alembic подключён к моделям, первая миграция применена
- Проверено: все таблицы созданы в `carpool.db`

## ✅ Шаг 4: Очередь (чтение) — GET /queue
- JOIN `QueuePosition` + `User`, сортировка по `position`
- Скрипт `scripts/seed.py` для тестовых пользователей
- Проверено локально через браузер и `/docs`

## ✅ Шаг 5: Создание заказа — POST /orders
- Принимает `route` и `comment` (опциональны)
- Создаёт `Order` со статусом `pending`
- Уточнение: сразу создаёт `OrderOffer` для первого в очереди
  (см. ARCHITECTURE.md)
- Проверено локально через `/docs`

## ✅ Шаг 6: Принятие/отказ — POST /orders/{id}/respond
- Принимает `user_id` и `response` (`accepted`/`declined`)
- При `accepted`: заказ назначается, пользователь сразу уходит в конец очереди
- При `declined`: оффер уходит следующему по очереди (по кругу)
- Проверено локально через `/docs`, оба сценария корректны

## ✅ Шаг 7: Завершение заказа — POST /orders/{id}/complete
- Проверяет `Order.status == assigned`, иначе `400`
- При успехе: `status -> completed`, `completed_at = now()`
- Рефакторинг ошибок: переход на `HTTPException` вместо `{"error": ...}`
  (см. ARCHITECTURE.md)
- Проверено локально через `/docs` и `curl`, все edge-кейсы корректны

## ✅ Шаг 8: История заказов — GET /orders
- Query-параметры: `status`, `user_id`, `limit` (default=100, max=500)
- Сортировка `created_at DESC`, без полноценной пагинации
- Проверено локально через `curl`: без фильтров и с разными комбинациями

## ✅ Шаг 9.1: GET /orders/pending (бэкенд)
- Read-only эндпоинт: `pending`-заказы + данные активного `OrderOffer`
- Аддитивное изменение, ничего не сломало
- Проверено вручную через `curl` полным сценарием (создание → отклонение →
  переход оффера → принятие → уход из очереди)

## ✅ Шаг 9.2: подключение статики в FastAPI
- `app.mount("/static", StaticFiles(...), name="static")`
- Структура папок `static/`, `static/css/`, `static/js/`
- Проверено: `GET /static/` отдаёт HTML, `GET /` — health-check JSON

## ✅ Шаг 9.3: дэшборд

Реализован по мини-шагам:

- ✅ **9.3.1** — HTML-каркас, селектор пользователя (`localStorage`),
  вывод очереди из `GET /queue`, подсветка текущего пользователя
- ✅ **9.3.2** — форма создания заказа (`POST /orders`), список активных
  предложений (`GET /orders/pending`), обновление без перезагрузки страницы
- ✅ **9.3.3** — кнопки принять/отклонить (показываются только если оффер
  адресован выбранному пользователю) и завершить (список "мои заказы
  в пути" через `GET /orders?status=assigned&user_id=X`)

## ✅ Шаг 9.4: страница истории заказов

- Создана `static/history.html` — отдельная страница с таблицей всех заказов
- Фильтры: `status` (select с вариантами pending/assigned/completed)
  и `user_id` (select с именами из `GET /queue`), применяются кнопкой
- Таблица использует уже готовый `GET /orders` с query-параметрами,
  бэкенд не менялся
- Взаимные ссылки между дэшбордом и историей в шапке ("История заказов →" / "← Дэшборд")
- Проверено: фильтры по отдельности и вместе работают корректно


## ✅ Шаг 9: Простой интерфейс (HTML) — завершён

### Разбивка на подшаги
- ✅ **9.1** — `GET /orders/pending` (бэкенд)
- ✅ **9.2** — подключение статики
- ✅ **9.3** — дэшборд (очередь, создание заказа, принять/отклонить/завершить)
- ✅ **9.4** — страница истории (таблица + фильтры)

## ✅ Шаг 10: Подготовка проекта к первому релизу — завершён

### Разбивка на подшаги
- ✅ **10.1** — русификация пользовательского интерфейса
- ✅ **10.2** — обновление README.md
- ✅ **10.3** — обновление PROGRESS.md
- ✅ **10.4** — создание CHANGELOG.md
- ✅ **10.5** — финальная проверка проекта перед деплоем

### Выполнено

- ✅ Полностью русифицирован пользовательский интерфейс
  (интерфейс, сообщения, статусы — единая терминология "водитель").
- ✅ Обновлён `README.md` перед первым релизом (описание, бизнес-логика,
  структура проекта, инструкция запуска, таблица API, ссылки на
  PROGRESS.md/ARCHITECTURE.md).
- ✅ Создан `CHANGELOG.md` (формат Keep a Changelog), первая версия
  зафиксирована как `0.1.0`.
- ✅ Финальная проверка по чеклисту (Шаг 10.5):
  - код и структура — чисто, без debug-кода и мёртвых веток;
    `.gitignore` и `.env.example` в порядке
  - БД/миграции — `alembic upgrade head` и `scripts/seed.py`
    проверены на чистой базе с нуля
  - полный сквозной прогон API-сценария (создание заказа → отказ →
    переход оффера по кругу → принятие → уход в конец очереди →
    завершение → фильтрация в истории) — все шаги корректны
  - веб-интерфейс (дэшборд + история) проверен вручную в браузере
  - документация сверена с кодом; в `ARCHITECTURE.md` найдена и
    исправлена порча кодировки (несколько символов)
  - `git status` чистый, всё запушено
  - создан и запушен тег релиза **`v0.1.0`**

### Проект зафиксирован как релиз v0.1.0 (2026-07-12)

## ✅ Шаг 11: деплой на VPS — завершён

- Сервер: Ubuntu 24.04 LTS, `109.248.160.140`, домен `zakaz.glorden.ru`
- Решение по техническому долгу (identity-проверки в `/respond` и
  `/complete`): оставлен как есть — осознанное упрощение для доверенной
  группы из 10 человек (см. ARCHITECTURE.md), деплой не блокирует
- Система обновлена (`apt update && upgrade`), перезагрузка применена
- Приложение работает под отдельным непривилегированным пользователем
  `deploy`, не под root
- Стек: systemd (автозапуск + restart on failure) + Caddy (reverse proxy,
  автоматический HTTPS через Let's Encrypt)
- Firewall (`ufw`): открыты только 22/80/443, порт приложения (8000)
  наружу не смотрит
- Доступ к приватному репозиторию — через read-only GitHub deploy key,
  сгенерированный на сервере
- На сервере — чистая БД (миграции `alembic upgrade head` с нуля,
  локальные тестовые данные не переносились)
- Проверено: `https://zakaz.glorden.ru/` (health-check), дэшборд и
  история открываются в браузере, `GET /queue` отвечает
- Полная инструкция по установке и обновлению — [DEPLOY.md](./DEPLOY.md)

### Проект зафиксирован как задеплоенный на прод (2026-07-22)

## ✅ Шаг 12: страница «Прайс» и редактируемый прайс-лист — завершён

- Добавлена страница `static/price.html`: туры, направления (102 шт.),
  бизнес-класс, минивэны — изначально статика, затем перенесено в БД
- Единая навигация между страницами вынесена в `static/js/nav.js`
  (одна строка под заголовком на каждой странице), добавлен
  `STYLEGUIDE.md` с базовыми соглашениями
- Живой поиск по названию направления на клиенте, без бэкенда
- HTTP Basic Auth в Caddy закрывает весь сайт целиком, включая все
  API-эндпоинты (проверено на `/queue`, `/orders`, `/users`, `/health`)
- Прайс переведён из статического HTML в БД: модели `PriceItem` и
  `PriceLogEntry`, CRUD-эндпоинты `GET/POST/PUT/DELETE /price` —
  добавлять/менять/удалять позиции может любой пользователь (та же
  модель доступа, что у `/respond`/`/complete`, см. ARCHITECTURE.md)
- Каждое изменение логируется (`GET /price/log`) и видно прямо на
  странице «Прайс» в свёрнутом блоке «История изменений»
- Data-миграция перенесла в БД текущие 112 позиций без потерь

## ✅ Шаг 13: отмена заказа — завершён
- `POST /orders/{id}/cancel`: работает для `pending`/`assigned`,
  из `completed`/`cancelled` — `400`
- Если заказ был `assigned` — водитель возвращается в **начало** очереди
  (не туда, откуда ушёл), см. ARCHITECTURE.md
- Кнопка «Отмена заказа» — и в блоке «Заказы, ожидающие ответа», и в
  «Мои заказы в пути» (одна и та же модель доступа, что у `/respond`/`/complete`)
- Добавлен статус `cancelled` в фильтр и таблицу страницы истории

## ✅ Шаг 14: уведомления водителям через VK — завершён
- Бэкенд: `app/notifications.py`, вызывается при создании заказа
  и при переходе оффера следующему по очереди (см. ARCHITECTURE.md)
- Сообщество «Горизонт. Едем далеко» добавлено участником в чат
  «Заказ» (там же постепенно собираются все водители)
- Особенность VK, с которой столкнулись: беседа не видна боту через
  API, пока в ней не отправлено хотя бы одно сообщение после
  добавления бота — учитывать при подключении новых чатов в будущем
- `VK_GROUP_TOKEN` и `VK_PEER_ID` настроены в `.env` на проде
- Живой тест на проде пройден: заказ создан → сообщение "Заказ #3
  (...) предложен: Антон Саныч" пришло в чат «Заказ» → тестовый
  заказ отменён, очередь не затронута
- Доработка: в сообщение добавлена ссылка на сайт (`SITE_URL`),
  водитель тегается по `User.vk_id`, если он известен (миграция
  `296fad1e2c65`); `vk_id` для всех 9 пользователей на проде
  сопоставлен по имени с участниками чата «Заказ»

## ✅ Шаг 15: ревью архитектуры + точечные правки — завершён
- Проведён полный обзор кода и документации (см. ARCHITECTURE.md,
  README.md, PROGRESS.md, CHANGELOG.md, DEPLOY.md на соответствие коду)
- Исправлено: `static/js/history.js` строил строки таблицы через
  `innerHTML` с прямой подстановкой `route`/`comment` — потенциальный
  stored XSS через свободный текст заказа. Переписано на
  `createElement`/`textContent`, как уже было в `dashboard.js`
- Исправлено: `OrderStatus.unassigned` был мёртвым кодом — ни один
  сценарий его не выставлял (отказ всегда уходит по кругу следующему).
  Убран из `app/models/order.py` и всех упоминаний (README/ARCHITECTURE/
  фильтр истории `history.html`/`history.js`). БД не требует миграции —
  колонка `status` в SQLite хранится как `VARCHAR`, без `CHECK`-ограничения
- Убрано отображение `(позиция N)` рядом с именем в очереди на дэшборде
  (`static/js/dashboard.js`) — номер и так виден из нумерации `<ol>`,
  а сырое значение `position` не идёт подряд без пропусков и путало
- Почищен `CHANGELOG.md`: убраны буквальные экранирующие слэши
  (`\#`, `\-`, `\[`), формулировки сокращены, детали — в PROGRESS.md

## ✅ Шаг 16: план identity-долга, бэкап БД, тесты на очередь — завершён
- ARCHITECTURE.md: явно сужен охват плана устранения identity-долга —
  только `/respond`/`/complete`; `/cancel`/`/price` вне охвата
  (обоснование см. в ARCHITECTURE.md). Добавлен раздел «на бумаге» —
  что понадобится проверить в 5 местах и что почти наверняка потребуется
  поле `Order.created_by`, если/когда появится полноценная авторизация
- Бэкап БД: `scripts/backup_db.sh`, ежедневный `sqlite3 .backup` через
  cron на проде. Хранение — гибрид: свежая копия всегда, более старые —
  по сетке «раз в 10 дней» от фиксированной даты (не растёт бесконечно,
  но остаются срезы за все периоды). Настройка cron — в DEPLOY.md.
  Живой тест на проде: обнаружилось, что CLI `sqlite3` не был установлен
  на сервере (в списке пакетов Шага 11 его не было — используется только
  Python-модуль `sqlite3`, а не сама утилита); доустановлен через `apt`,
  добавлен в список пакетов DEPLOY.md на будущее. Крон установлен и
  проверен вручную (`crontab -l`, ручной запуск скрипта — бэкап создан)
- Добавлены первые автотесты: `tests/test_queue_logic.py` (+
  `tests/conftest.py` с изолированной SQLite-БД на тест через
  `tmp_path` и подменой `get_session`). Покрыта самая хрупкая логика:
  принятие → в конец очереди, отказ → по кругу следующему (включая
  разворот с последнего на первого), отмена принятого → возврат вторым,
  не обгоняя текущего первого. `pytest`/`httpx` добавлены в
  `requirements.txt`, все 3 теста проходят локально

## ✅ Шаг 17: предупреждение о латинице в маршруте, переработка VK-уведомлений — завершён
- Под полем «Маршрут» на дэшборде появляется подсказка «Проверьте
  раскладку — маршрут латиницей», если введены латинские буквы
  (`static/js/dashboard.js:checkRouteLatin`, слушатель на `input`) —
  не блокирует отправку; решили не автоконвертировать транслит из-за
  риска ошибок
- Фикс: класс `.hint-warning` задавал `display: block`, что перебивало
  атрибут `hidden` (авторские стили сильнее UA-стилей независимо от
  специфичности) — подсказка показывалась даже на пустом поле при
  загрузке страницы. Добавлено `.hint-warning[hidden] { display: none }`
- Текст VK-уведомления о новом предложении переработан в многострочный
  формат: заголовок, маршрут отдельной строкой, комментарий к заказу
  (раньше вообще не попадал в сообщение), приглашение взять заказ
- При отказе от заказа уведомление о следующем предложении теперь
  называет, кто отказался («Антон Саныч отказался.») вместо
  нейтрального «Новый заказ»
- При принятии заказа отправляется отдельное VK-уведомление «Имя взял
  заказ №N.» (`notify_accepted` в `app/notifications.py`); общая логика
  отправки вынесена в `_send()`, чтобы не дублировать код запроса к VK API
- Живой тест на проде: реальный тестовый заказ прошёл сценарий
  отказ → отказ → принятие, все 4 уведомления ушли в реальный VK-чат
  «Заказ». Заказ и изменения очереди полностью откачены вручную (заказ
  удалён из истории, позиции очереди восстановлены прямым SQL до
  исходных значений; бэкап БД сделан заранее)
- `tests/test_queue_logic.py` прогнан повторно — все 3 теста проходят;
  пробелы в покрытии (граничные случаи, ошибочные пути) зафиксированы,
  не устранялись в рамках этого шага

## ✅ Шаг 18: курс на будущее — routers/services при росте main.py — завершён
- Обсуждение: весь роутинг сейчас в одном `app/main.py` без
  `APIRouter`, бизнес-логика (машина состояний заказа, математика
  очереди) написана прямо в теле хендлеров — `respond_to_order`
  (86 строк), `cancel_order` (40 строк). Рефакторить сейчас признано
  преждевременной абстракцией для масштаба 10 пользователей
- В ARCHITECTURE.md добавлен раздел «Разделение бизнес-логики и
  FastAPI (задел на будущее, только на бумаге)»: `app/routers/*.py`
  как тонкий транспортный слой, `app/services/*.py` — вся бизнес-логика.
  Формулировка: роутеры — двери в систему (сегодня REST API, возможно
  позже Telegram-бот или другой интерфейс), сервисы — сама система;
  правила очереди и заказов остаются в одном месте независимо от числа
  дверей
- Ничего не реализовано — только зафиксировано правило на будущее:
  новая логика сложнее пары строк сразу пишется как router+service,
  а не переносится задним числом

## ✅ Шаг 19: журнал действий — GET /activity — завершён
- По GitHub issue: отдельная сущность `ActivityLog` (`app/models/activity.py`)
  — `user_id`/`order_id` nullable, `event_type`, `message`, не заменяет
  историю заказов (`Order`), а дополняет её событиями по ходу жизни
  заказа. Миграция `8f2a1c9d4b3e`
- Событие пишется в `create_order`, `respond_to_order` (оба ветвления),
  `complete_order`, `cancel_order`: `order_created`, `order_offered`,
  `order_accepted`, `order_declined`, `order_completed`, `order_cancelled`,
  `queue_changed`. `user_id` не всегда известен (у `create`/`complete`/
  `cancel` нет тела запроса с исполнителем — см. identity-долг в
  ARCHITECTURE.md), тогда пишется `None`
- `GET /activity` — фильтры `user_id`/`order_id`/`event_type`, `limit`
  (default=100, max=500), новые сверху — по образцу `GET /price/log`
- Страница «Журнал действий» (`static/activity.html` + `activity.js`),
  добавлена в `nav.js`
- **Побочный баг, найденный и исправленный по пути:** `_log_activity`
  коммитит сессию, что инвалидирует (`expire`) уже загруженный объект
  `order`/`item` в SQLAlchemy; при возврате такого объекта как
  `response_model` FastAPI сериализовал его в пустой `{}` без ошибки
  (Pydantic v2 берёт значения из `__dict__` инстанса, а `expire` их
  оттуда убирает — lazy-reload не срабатывает на этом пути). Тот же
  баг уже существовал в `POST/PUT /price` (log-entry коммитится после
  `refresh(item)` и не рефрешится назад) — просто был незаметен, т.к.
  фронтенд не читает поля из ответа `POST/PUT`, а после `POST /orders`
  дэшборд показывает `order.id` в статусе, и `id` стал `undefined`.
  Исправлено добавлением `session.refresh(order)`/`session.refresh(item)`
  после каждого коммита лога, перед `return`
- Тесты: `tests/test_activity_log.py` — полный жизненный цикл заказа
  пишет ожидаемую последовательность событий, отмена назначенного
  заказа пишет `queue_changed`+`order_cancelled`, фильтры по
  `event_type`/`user_id` работают. Все 6 тестов (3 старых + 3 новых)
  проходят
- Миграция проверена на копии `carpool.db` в scratchpad: upgrade/downgrade/
  upgrade — чисто, без ошибок; применена на локальной dev-БД
- Живая проверка в браузере (dev-сервер): создание заказа → дэшборд
  корректно показывает `Заказ №N создан` (баг с `undefined` исчез),
  на странице «Журнал действий» видны все события в правильном порядке

## ✅ Шаг 20: самоназначение на заказ — `POST /orders/{id}/self-assign` — завершён
- По GitHub issue: водитель может взять `pending`-заказ вне очереди
  (например, если уже находится в городе подачи), указав обязательную
  причину. Сразу становится исполнителем и уходит в конец очереди —
  тем же переходом, что при обычном принятии (`max_position + 1`);
  существующая логика распределения заказов по очереди не изменена
- `Order.self_assign_reason` (миграция `ed3788bb5a0a`) хранит причину;
  видна отдельной колонкой в «Истории заказов» и в тексте нового события
  `order_self_assigned` в «Журнале действий» (`app/main.py`,
  `app/notifications.py:notify_self_assigned`)
- Если у заказа была активная офера кому-то другому — помечается
  `declined`, чтобы не висела; позиция предложенного водителя в очереди
  не меняется
- Проверка, что `user_id` состоит в очереди (`400` иначе) — на `/users`
  бывают диспетчеры без `QueuePosition`, без проверки самоназначение
  такого пользователя падало бы `500`
- Кнопка «Самоназначиться» на дэшборде (`static/js/dashboard.js`) —
  показывается на заказах, не предложенных текущему выбранному
  пользователю; причина запрашивается через `prompt()`, пустая/отменённая
  причина не отправляет запрос
- Тесты: `tests/test_queue_logic.py` (перемещение в конец очереди, пустая
  причина → `400`, уже назначенный заказ → `400`, пользователь вне
  очереди → `400`) и `tests/test_activity_log.py` (причина и `queue_changed`
  пишутся в журнал). Все 11 тестов проходят
- Миграция проверена на копии `carpool.db` в scratchpad: upgrade/downgrade/
  upgrade — чисто; применена на локальной dev-БД
- Живая проверка в браузере (dev-сервер): кнопка появляется на нужных
  заказах, после самоназначения водитель виден в конце очереди
  (`GET /queue`), причина отображается в «Истории заказов» и в «Журнале
  действий» с новым фильтром «Самоназначение на заказ». Проверочный
  заказ (№14) в dev-БД оставлен в статусе `assigned` — не откатывался,
  как и остальные тестовые данные в этой базе

## ✅ Шаг 21: скрипт для ручной перестановки очереди — завершён
- Обсуждение: понадобилось иногда поправлять порядок в очереди вручную.
  Решили не строить UI/эндпоинт — редкая операция, доверенная группа
  из 10 человек, доступ к серверу есть; по образцу `scripts/add_user.py`
- `scripts/reorder_queue.py username1 username2 ...` — принимает полный
  список username в нужном порядке, требует, чтобы набор пользователей
  совпадал ровно с текущим составом очереди (без пропусков и лишних),
  иначе понятная ошибка и выход без изменений в БД. Переставляет
  `QueuePosition.position` на `0, 1, 2, ...` в переданном порядке
- Без автотестов (как и `add_user.py`/`seed.py`) — проверено вручную на
  локальной dev-БД: дубликат username, несуществующий username,
  неполный список (не хватает одного из очереди) — все три ветки дают
  понятную ошибку и `exit(1)`, ничего не меняя в БД; успешная
  перестановка (Иван/Мария местами) отражается в `QueuePosition`,
  проверено напрямую через БД. После проверки порядок в dev-БД вручную
  возвращён к исходному

## ✅ Шаг 22: статистика — `GET /statistics/summary` — завершён
- По GitHub issue (написан давно, часть формулировок устарела —
  зафиксировано в ARCHITECTURE.md): отдельная страница «Статистика» с
  общими счётчиками по заказам и разбивкой по водителям
- Один эндпоинт `GET /statistics/summary`, один проход по всем заказам
  (без пагинации, как `GET /orders`) — `overall`
  (`total`/`completed`/`active`/`cancelled`, `active` = `pending` +
  `assigned`) и `by_driver` (только те, кто сейчас в очереди —
  `received`/`completed`/`cancelled` на каждого)
- В процессе планирования решили не сводить отменённые после
  назначения заказы молча в `received` — добавили отдельную колонку
  `cancelled` по водителю (3 колонки вместо 2 из исходного issue)
- Страница `static/statistics.html` + `statistics.js` — общая
  статистика простым списком, по водителям — таблица с адаптивным CSS
  (по образцу `#history-table`, продублирован под `#driver-stats-table`,
  без обобщения в общий класс — см. ARCHITECTURE.md). Ссылка добавлена
  в `nav.js` последней в меню
- Тесты: `tests/test_statistics.py` — общие счётчики и инвариант
  `total == completed + active + cancelled`, разбивка по водителям
  включая self-assign (учитывается наравне с обычным принятием),
  отменённый после назначения заказ (`received`+`cancelled`, не
  `completed`), сортировка по убыванию `received`, диспетчер без
  `QueuePosition` не попадает в `by_driver`. Все 16 тестов проходят
  (5 новых + 11 старых)
- Эндпоинт полностью read-only — критерий приёмки «существующая логика
  очереди и распределения заказов не изменяется» выполнен по
  построению, без изменений моделей/миграций

## ✅ Шаг 23: вторая очередь — дальние/короткие поездки — завершён
- До сих пор в проекте была одна очередь. Завели вторую, полностью
  независимую: `QueueType` (`long`/`short`) в `app/models/queue.py`.
  `QueuePosition` теперь уникальна по паре `(user_id, queue_type)`
  вместо одного `user_id` — один и тот же водитель может состоять в
  обеих очередях одновременно с независимыми позициями. `Order`
  получил обязательное поле `queue_type` — тип заказа выбирается на
  дэшборде радиокнопками «Дальний»/«Короткий» без значения по
  умолчанию (`required`, форма не даст создать заказ без явного
  выбора) — никакого автоопределения по маршруту не делали
- Вся арифметика очереди (`create_order`, обе ветки `respond_to_order`,
  `self_assign_order`, `cancel_order` в `app/main.py`) теперь
  фильтрует `QueuePosition` по `queue_type` заказа — заказ одного типа
  затрагивает позиции только в своей очереди. Самое чувствительное
  место — переиндексация в `cancel_order` (раньше шла по всей таблице
  `QueuePosition`, без фильтра захватила бы обе очереди разом)
- Миграция `5437dcec06ba`: первая в проекте, где SQLite потребовал
  `batch_alter_table` сразу на двух таблицах (`order` — переход
  `queue_type` в `NOT NULL` после backfill; `queueposition` — то же
  плюс замена constraint). Существующий `UNIQUE(user_id)` оказался
  анонимным (без имени) — `drop_constraint` внутри batch-режима нашёл
  его только через явный `naming_convention`. Короткая очередь при
  миграции — клон дальней (тот же состав и порядок на старте, дальше
  независимо). Проверено эмпирически на копии `carpool.db`:
  upgrade → downgrade → upgrade, все три раза чисто
- Побочный баг, найденный и исправленный по пути: `GET /statistics/summary`
  строил `by_driver` через join `QueuePosition`+`User` — как только
  водитель оказывается в обеих очередях, он попадал в таблицу статистики
  дважды с одинаковыми числами. Исправлено на `select(User)...distinct()`
- `GET /queue` — опциональный `queue_type`: без параметра отдаёт обе
  очереди сразу (для `history.js`/`activity.js`, которым нужен просто
  список водителей), с параметром — порядок одной конкретной очереди
  (для двух блоков на дэшборде). Без параметра один и тот же водитель
  может прийти дважды — `populateUserFilter` в `history.js`/`activity.js`
  дедуплицирует по `user_id`
- Дэшборд (`static/index.html`/`dashboard.js`): блок «Очередь» разделён
  на «Дальние поездки»/«Короткие поездки»; секции «Ожидают ответа» и
  «Мои заказы в пути» остались едиными списками — тип заказа показан
  компактным цветным бейджем рядом с маршрутом (переиспользует уже
  существующие цвета кнопок — синий/оранжевый). «История заказов»
  получила колонку и фильтр «Тип», симметрично `status`
- `scripts/add_user.py` по умолчанию добавляет нового водителя в обе
  очереди (флаг `--queue-type=long|short` — только в одну);
  `scripts/reorder_queue.py` принимает тип очереди первым позиционным
  аргументом и переставляет только её
- VK-уведомления (`app/notifications.py`) теперь называют тип поездки
  в тексте сообщения — единственный канал, где иначе неоткуда было бы
  узнать, о какой из двух очередей речь
- Тесты: `tests/conftest.py` — `seed_queue` принимает `queue_type`
  (дефолт `long`, все старые вызовы не тронуты по смыслу) + новый
  хелпер `add_to_queue` для сценариев, где один состав сидит в обеих
  очередях. 6 новых тестов в `tests/test_queue_logic.py`, специально
  сажающих одних и тех же людей в `short` в другом порядке, чем в
  `long`, чтобы поймать пропущенный фильтр: принятие/отказ-по-кругу/
  самоназначение/отмена в одной очереди не двигают другую, `create_order`
  предлагает только внутри своего типа, отсутствие `queue_type` → `422`.
  Плюс механическая правка ~21 существующего вызова `POST /orders` в
  трёх тестовых файлах (добавлен `"queue_type": "long"`). Все 22 теста
  проходят
- Живая проверка в браузере (dev-сервер): создание короткого заказа →
  предложен первому в короткой очереди → принятие → водитель уходит в
  конец именно короткой очереди, дальняя не меняется (и наоборот для
  дальнего заказа) — проверено в обе стороны на реальных данных.
  Фильтр «Тип» на «Истории заказов» и таблица «Статистика» (без
  дублей после фикса) проверены вручную. Нет горизонтального
  переполнения на 375px, XSS-защита в истории заказов не пострадала
- **Два визуальных фикса сразу после первого прод-деплоя** (по
  скриншоту пользователя): 1) блоки «Дальние поездки»/«Короткие
  поездки» были друг под другом — обёрнуты в `.queue-columns`
  (`display: flex`, ряд на десктопе, колонка на `≤640px`, по образцу
  остальных адаптивных блоков в `style.css`); 2) радиокнопки типа
  заказа визуально «разъезжались» — причина: старое правило
  `#create-order-section input { width: 100%; ... }`, писавшееся под
  текстовые поля «Маршрут»/«Комментарий», случайно растягивало и
  `type="radio"` на всю ширину контейнера. Сужено до
  `input[type="text"]`. Проверено вычислением `getBoundingClientRect`
  (блоки очередей на одной высоте, радио и текст выровнены по центру
  с точностью до 0.5px), не только на глаз

## ✅ Шаг 24: перенос на новый VPS — завершён
- Причина: смена провайдера/сервера, инициирована пользователем
  (без инцидента — прежний VPS продолжал исправно работать)
- Новый сервер: Ubuntu 24.04 LTS, `85.159.231.189`, тот же домен
  `zakaz.glorden.ru`. Старый (`109.248.160.140`) временно оставлен
  включённым как резерв, сервис на нём остановлен после переноса БД
- Установка нового сервера прогнана по DEPLOY.md «с нуля»: пакеты,
  непривилегированный пользователь `deploy`, Caddy, `ufw` (22/80/443),
  новый read-only GitHub deploy-key (сгенерирован заново, не
  переносился со старого сервера), клонирование, venv, systemd,
  cron бэкапов
- `.env` перенесён со старого сервера один в один (там боевой
  `VK_GROUP_TOKEN`/`VK_PEER_ID`, которые нельзя перевыпустить),
  кроме `SECRET_KEY` — сгенерирован новый, отдельный для этого
  сервера (значение фактически нигде не используется в коде, так что
  без последствий)
- Basic Auth в Caddyfile перенесён тем же bcrypt-хэшем, что и на
  старом сервере — логин/пароль для пользователей не поменялся
- Перенос БД: короткое окно простоя — сервис на старом сервере
  остановлен, `carpool.db` скопирован напрямую (`ssh ... cat | ssh
  ... cat >`), проверено количество строк (`user`/`order`) после
  переноса, `alembic upgrade head` — без новых миграций (БД уже на
  актуальной версии), сервис запущен на новом сервере
- DNS: A-запись `zakaz.glorden.ru` переключена пользователем у
  регистратора на новый IP. Пропагация оказалась неравномерной —
  проверка через whatsmydns.net показала новый IP почти повсеместно
  (включая Россия) намного раньше, чем через часть резолверов
  (8.8.8.8/1.1.1.1 и даже часть authoritative NS домена с конкретного
  сетевого пути, ISP пользователя) — не проблема конфигурации,
  а обычная неравномерность DNS-кеширования
- Caddy на новом сервере автоматически получил боевой сертификат
  Let's Encrypt (`tls.obtain: certificate obtained successfully`,
  `acme-v02` — не staging) сразу после того, как домен стал резолвиться
  на новый IP
- Проверено: `curl --resolve` напрямую на новый IP отдавал ожидаемый
  `401` (Basic Auth) до завершения DNS-пропагации; пользователь лично
  подтвердил рабочий сайт в браузере после пропагации

### Хвосты, дозавершено 2026-07-24
- Перед выключением старого сервера обнаружилось, что его
  `carpool-queue.service` снова был `active` (сервер, похоже,
  самостоятельно перезагрузился — юнит остался `enabled` после
  первого `stop` и поднялся заново при старте системы). Данные не
  разошлись: на старом сервере как было, так и осталось 9 заказов
  с момента переноса, новых через него никто не создавал — DNS уже
  вёл весь трафик на новый сервер. Сервис на старом остановлен
  и в этот раз ещё и `disable`
- Остальные бэкапы БД со старого сервера (плюс снэпшот, снятый прямо
  перед переносом) скопированы в `backups/` на новом — не потеряны
- Локальные SSH-алиасы на машине пользователя: `carpool-vps`/
  `carpool-vps-deploy` теперь снова означают боевой сервер (указывают
  на `85.159.231.189`), старый сервер доступен под `carpool-vps-old`/
  `carpool-vps-old-deploy`
- Старый read-only GitHub deploy-key отозван пользователем в
  настройках репозитория; новый ключ (сгенерированный на новом
  сервере) проверен ещё раз после отзыва — `git fetch` отработал
- Старый сервер выключен (`shutdown -h now`) — данные на нём физически
  целы, но SSH/сервис недоступны без включения через панель
  хостинг-провайдера
- **Инцидент в параллельной сессии:** пока переименование алиасов ещё
  не было сделано, в другой, одновременно открытой сессии Claude Code
  кто-то выполнил `scripts/reorder_queue.py` и рестарт сервиса через
  ещё старые на тот момент алиасы `carpool-vps`/`carpool-vps-deploy`
  — они тогда указывали на уже выведенный из-под DNS старый сервер,
  а не на боевой. Пользователь заметил, что живой сайт не отражает
  изменения, переспросил ту сессию — там разобрались сами.
  Итог сверен и здесь: очередь на боевом сервере после этого
  инцидента осталась в правильном, ожидаемом состоянии, руками
  ничего пересобирать не потребовалось. Урок на будущее: после любой
  инфраструктурной миграции сверять целевой хост по DNS/DEPLOY.md,
  а не только по названию SSH-алиаса — старый сервер может оставаться
  доступным и правдоподобно отвечать ещё долго после того, как
  перестал быть прод