# Carpool Queue

Внутренний сервис для организации очереди водителей, которые по очереди берут заказы на межгородние поездки. Решает простую задачу: кто из группы водителей едет следующим, кто уже отказался, а кто завершил поездку — без ручного учёта в чатах и таблицах.

Разработан для доверенной группы примерно из 10 человек.

## Возможности

- Очередь водителей с автоматическим порядком (`GET /queue`)
- Создание заказа с автоматическим предложением первому в очереди (`POST /orders`)
- Принятие или отказ от предложенного заказа (`POST /orders/{id}/respond`)
- Завершение поездки (`POST /orders/{id}/complete`)
- История всех заказов с фильтрами по статусу и пользователю (`GET /orders`)
- Отображение активных предложений — кому и когда сейчас предложен заказ (`GET /orders/pending`)
- Веб-интерфейс на русском языке: дэшборд с очередью и созданием заказов + отдельная страница истории

## Стек технологий

- **Backend:** Python 3.13, FastAPI, SQLModel, SQLite, Alembic
- **Frontend:** HTML, CSS, vanilla JavaScript (без фреймворков и шаблонизаторов)
- **Инфраструктура (план на Шаг 11):** Ubuntu Server, Caddy, systemd

## Как это работает (бизнес-логика)

1. Кто-то создаёт заказ — он **сразу** автоматически предлагается первому в очереди.
2. Первый в очереди либо **принимает** заказ (сразу уходит в конец очереди, чтобы новый заказ не пришёл ему повторно, пока он занят), либо **отказывается** (остаётся на своей позиции, заказ автоматически уходит следующему по кругу).
3. После завершения поездки водитель отмечает заказ как **завершённый**.
4. Таймаутов на ответ нет — водители сами договариваются в личном чате, сколько ждать.

Подробности и обоснование этих решений — в [ARCHITECTURE.md](./ARCHITECTURE.md).

## Структура проекта
carpool-queue/
├── app/
│   ├── main.py            # точка входа FastAPI, регистрация роутов
│   ├── config.py           # настройки приложения
│   ├── database.py         # подключение к БД (SQLite)
│   └── models/
│       ├── user.py         # модель User
│       ├── queue.py         # модель QueuePosition
│       └── order.py         # модели Order и OrderOffer
├── alembic/                # миграции БД
│   └── versions/
├── static/
│   ├── index.html           # дэшборд (очередь + создание заказа)
│   ├── history.html         # история заказов
│   ├── css/style.css
│   └── js/
│       ├── dashboard.js
│       └── history.js
├── scripts/
│   └── seed.py              # наполнение БД тестовыми данными
├── requirements.txt
├── alembic.ini
├── .env.example
├── PROGRESS.md              # журнал выполнения этапов
├── ARCHITECTURE.md          # архитектурные решения и технический долг
└── README.md
## Установка и запуск

### Первоначальная настройка (один раз)

```cmd
cd C:\Users\Oleg\Desktop\carpool-queue
python -m venv .venv
.venv\Scripts\activate.bat
pip install -r requirements.txt
copy .env.example .env
alembic upgrade head
```

Последняя команда создаст файл `carpool.db` со всеми нужными таблицами.

### Запуск (Windows, cmd.exe)

```cmd
cd C:\Users\Oleg\Desktop\carpool-queue
chcp 65001
.venv\Scripts\activate.bat
uvicorn app.main:app --reload
```

Проверка: [http://127.0.0.1:8000](http://127.0.0.1:8000) должен вернуть:
```json
{"status": "ok", "message": "Carpool queue service is running"}
```

Веб-интерфейс: [http://127.0.0.1:8000/static/index.html](http://127.0.0.1:8000/static/index.html)

<details>
<summary>Запуск на Linux/macOS</summary>

```bash
source .venv/bin/activate
uvicorn app.main:app --reload
```

</details>

### Тестовые данные

Чтобы наполнить очередь тестовыми пользователями:

```bash
python -m scripts.seed
```

### Если менялись модели БД

```bash
alembic revision --autogenerate -m "описание изменения"
alembic upgrade head
```

## API

| Метод | Путь | Описание |
|---|---|---|
| `GET` | `/` | проверка работоспособности сервиса |
| `GET` | `/queue` | список пользователей в текущем порядке очереди |
| `POST` | `/orders` | создать новый заказ (`route`, `comment` — опциональны) |
| `POST` | `/orders/{order_id}/respond` | принять/отклонить предложенный заказ |
| `POST` | `/orders/{order_id}/complete` | отметить заказ завершённым (заказ должен быть в статусе `assigned`) |
| `GET` | `/orders` | история заказов; query-параметры `status`, `user_id`, `limit` (по умолчанию 100, максимум 500); сортировка по `created_at` (новые сверху) |
| `GET` | `/orders/pending` | заказы в статусе `pending` вместе с данными активного предложения — кому сейчас предложен заказ и когда |

Ошибки возвращаются в стандартном формате FastAPI: `{"detail": "..."}` с соответствующим HTTP-статусом (`404`/`400`).

Интерактивная документация API доступна по адресу `/docs` (Swagger UI) при запущенном сервере.

## Известные ограничения

`/orders/{id}/respond` и `/orders/{id}/complete` пока не проверяют identity вызывающего — любой `user_id` может принять/отклонить/завершить любой заказ. Это осознанное упрощение для доверенной группы из 10 человек, а не недосмотр. План устранения и обоснование — в [ARCHITECTURE.md](./ARCHITECTURE.md).

## Статус проекта

Проект в активной разработке. Backend и веб-интерфейс полностью рабочие и покрывают основной сценарий использования. Актуальный список выполненных и предстоящих шагов — в [PROGRESS.md](./PROGRESS.md).

## Документация

- [PROGRESS.md](./PROGRESS.md) — журнал выполнения этапов
- [ARCHITECTURE.md](./ARCHITECTURE.md) — архитектурные решения, логика очереди, технический долг, формат работы